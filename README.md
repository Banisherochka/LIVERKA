# 🏥 Система сегментации печени на КТ-снимках | Liver CT Segmentation System

> **Дообученная модель машинного обучения для автоматической сегментации печени на реальных КТ-снимках пациентов клиники города Екатеринбург с 3D-визуализацией и возможностью корректировки**

[![Python](https://img.shields.io/badge/Python-3.11-blue.svg)](https://python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-green.svg)](https://fastapi.tiangolo.com)
[![Angular](https://img.shields.io/badge/Angular-18-red.svg)](https://angular.io)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)](https://docker.com)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

---

## 📋 Описание проекта

Система разработана для клиники города Екатеринбург для автоматической сегментации печени на КТ-снимках реальных пациентов.

### Ключевые возможности:

✅ **Автоматическая сегментация** печени на КТ-снимках с использованием продвинутой 3D U-Net архитектуры  
✅ **Дообучение модели** на новых данных заказчика (КТ-снимки из клиники Екатеринбурга)  
✅ **3D-визуализация** результатов с возможностью интерактивного просмотра  
✅ **Редактирование 3D-модели** на веб-интерфейсе для корректировки ошибок нейросети  
✅ **Клинические метрики** качества: Dice (>0.90), IoU, Hausdorff Distance  
✅ **REST API** для интеграции с другими системами  
✅ **Docker-контейнеризация** для простого развертывания

---

## 🎯 Цели проекта

### Основная цель:
Дообучить модель машинного обучения по сегментации КТ-снимков печени на новых данных заказчика и упаковать решение в сервис с использованием 3D-технологий.

### Требуемый результат:
- Модель машинного обучения, распознающая реальные снимки заказчика
- Дообученная модель с пороговыми значениями метрик (Dice ≥ 0.90)
- Упакованное решение в Docker-контейнер
- Веб-интерфейс с 3D-визуализацией и редактированием моделей

### Критерии приемки:
1. ✅ Дообученная модель с Dice ≥ 0.90 на данных заказчика
2. ✅ Автоматическая обводка печени по контуру
3. ✅ Построение 3D-модели из сегментации
4. ✅ Возможность корректировки модели на веб-сайте
5. ✅ Упаковка в Docker-контейнер

---

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (Angular 18)                   │
│  • Upload Page (загрузка DICOM)                                 │
│  • Results Page (просмотр результатов)                          │
│  • 3D Viewer (Three.js - визуализация + редактирование)        │
└────────────────────────┬────────────────────────────────────────┘
                         │ REST API
┌────────────────────────┴────────────────────────────────────────┐
│                      Backend API (FastAPI)                      │
│  • Authentication (JWT)                                          │
│  • File Upload & Validation                                      │
│  • Segmentation Endpoints                                        │
│  • 3D Model Generation                                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
┌────────┴────────┐            ┌─────────┴─────────┐
│   PostgreSQL    │            │   Neural Network  │
│   (Database)    │            │   (3D U-Net)      │
└─────────────────┘            └───────────────────┘
```

---

## 📦 Структура проекта

```
liver-ct-segmentation/
├── backend/                    # Backend API (FastAPI + Python)
│   ├── app/
│   │   ├── api/               # API endpoints
│   │   │   └── v1/
│   │   │       ├── auth.py           # Аутентификация
│   │   │       ├── ct_scans.py       # КТ-сканы
│   │   │       ├── segmentations.py  # Сегментация
│   │   │       └── health.py         # Health check
│   │   ├── models/            # SQLAlchemy модели БД
│   │   ├── services/          # Бизнес-логика
│   │   │   ├── dicom_processing_service.py
│   │   │   ├── liver_segmentation_service.py
│   │   │   └── dicom_to_3d_service.py
│   │   ├── core/              # Безопасность, конфиг
│   │   ├── middleware/        # Middleware (security, rate limiting)
│   │   └── tasks/             # Celery задачи
│   ├── alembic/               # Миграции БД
│   ├── storage/               # Хранилище файлов
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── requirements.txt
│
├── frontend/                   # Frontend (Angular 18)
│   ├── src/
│   │   ├── app/
│   │   │   ├── pages/
│   │   │   │   ├── upload-page/      # Загрузка DICOM
│   │   │   │   ├── results-page/     # Результаты
│   │   │   │   └── login-page/       # Вход
│   │   │   ├── components/
│   │   │   │   ├── file-dropzone/    # Drag & drop
│   │   │   │   └── 3d-viewer/        # 3D визуализация (Three.js)
│   │   │   ├── services/
│   │   │   └── guards/
│   │   └── assets/
│   ├── angular.json
│   └── package.json
│
├── neural_network/             # Нейросеть (PyTorch)
│   └── python_services/
│       └── liver_segmentation/
│           ├── model.py              # 3D U-Net архитектура
│           ├── inference.py          # Инференс пайплайн
│           ├── preprocessing.py      # Предобработка DICOM
│           ├── metrics.py            # Метрики качества
│           └── requirements.txt
│
├── docker-compose.yml          # Полный Docker Compose
├── .dockerignore
├── .gitignore
└── README.md                   # Этот файл
```

---

## 🚀 Быстрый старт

### Вариант 1: Запуск с Docker (Рекомендуется)

```bash

docker build -t liver-segmentation-frontend .

# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps

# API будет доступен на http://localhost:8000
# Frontend на http://localhost:4200
```

### Вариант 2: Локальная разработка

#### Backend

```bash
cd backend

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Настроить .env файл
cp .env.example .env
# Отредактировать .env (DATABASE_URL, SECRET_KEY и т.д.)

# Применить миграции БД
alembic upgrade head

# Запустить сервер
python run.py
# или
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

API документация: http://localhost:8000/docs

#### Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Запустить dev server
npm start

# или
ng serve
```

Frontend: http://localhost:4200

#### Neural Network (опционально для дообучения)

```bash
cd neural_network/python_services

# Установить зависимости
pip install -r requirements.txt

# Дообучение на данных Екатеринбурга
python -c "
from liver_segmentation.model import fine_tune_on_yekaterinburg_data
pipeline, optimizer, scheduler, loss_fn = fine_tune_on_yekaterinburg_data(
    data_dir='../../Anon_Liver',
    epochs=50,
    learning_rate=1e-4
)
"
```

---

## 🧠 Нейронная сеть

### Архитектура: 3D U-Net с улучшениями

- **Базовая архитектура**: 3D U-Net
- **Улучшения**:
  - Residual connections (ResNet-style)
  - Attention Gates для лучшей фокусировки на печени
  - Deep Supervision для стабильного обучения
  - Dropout для регуляризации
  - Batch Normalization

### Характеристики модели:

- **Входные данные**: 3D КТ-снимки (1 канал, переменный размер)
- **Выходные данные**: Бинарная маска печени (вероятности [0,1])
- **Параметры**: ~15-20M (зависит от конфигурации)
- **Глубина**: 5 уровней
- **Начальное количество фильтров**: 64

### Метрики качества:

| Метрика | Порог | Описание |
|---------|-------|----------|
| **Dice Coefficient** | ≥ 0.90 | Степень перекрытия GT и предсказания |
| **IoU (Jaccard)** | ≥ 0.85 | Intersection over Union |
| **Hausdorff Distance 95** | ≤ 10 mm | Точность границ сегментации |
| **Sensitivity** | ≥ 0.90 | Доля правильно найденной печени |
| **Specificity** | ≥ 0.95 | Доля правильно исключенных фоновых вокселей |

### Дообучение на данных заказчика:

```python
from liver_segmentation.model import fine_tune_on_yekaterinburg_data

# Дообучение модели
pipeline, optimizer, scheduler, loss_fn = fine_tune_on_yekaterinburg_data(
    model_path="models/pretrained_liver_segmentation.pth",  # Предобученная модель
    data_dir="Anon_Liver/",                                  # КТ-снимки из клиники
    output_dir="models/fine_tuned/",
    epochs=50,
    learning_rate=1e-4
)
```

**Функция потерь**: Комбинированная `0.5 * Dice Loss + 0.5 * BCE Loss`

---

## 🔐 API Endpoints

### Authentication

```http
POST /api/v1/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "your_password"
}

Response: {"access_token": "jwt_token", "token_type": "bearer"}
```

### Health Check

```http
GET /api/v1/health

Response: {
  "status": "healthy",
  "database": "connected",
  "redis": "connected"
}
```

### Segmentation

#### Загрузить DICOM и запустить сегментацию

```http
POST /api/v1/segmentation/upload
Authorization: Bearer {token}
Content-Type: multipart/form-data

file: <dicom_file.dcm>

Response: {
  "success": true,
  "data": {
    "task_id": 1,
    "ct_scan_id": 1,
    "status": "pending",
    "message": "Segmentation task created successfully"
  }
}
```

#### Получить статус задачи

```http
GET /api/v1/segmentations/{task_id}
Authorization: Bearer {token}

Response: {
  "id": 1,
  "ct_scan_id": 1,
  "status": "completed",
  "progress": 100,
  "started_at": "2024-01-08T10:00:00",
  "completed_at": "2024-01-08T10:00:15"
}
```

#### Получить результаты сегментации

```http
GET /api/v1/segmentations/{task_id}/result
Authorization: Bearer {token}

Response: {
  "task_id": 1,
  "status": "completed",
  "metrics": {
    "dice": 0.934,
    "iou": 0.917,
    "volume_ml": 1259.5,
    "quality": "Excellent",
    "clinical_acceptable": true
  },
  "segmentation_file": "/storage/segmentations/seg_1.nii.gz",
  "3d_model_file": "/storage/3d_models/liver_1.stl"
}
```

#### Скачать 3D модель

```http
GET /api/v1/segmentations/{task_id}/download_3d_model
Authorization: Bearer {token}

Response: <STL file download>
```

---

## 🖥️ Frontend (Angular 18)

### Компоненты:

1. **Upload Page** (`/upload`)
   - Drag & drop для загрузки DICOM файлов
   - Валидация файлов (.dcm)
   - Прогресс загрузки

2. **Results Page** (`/results/:id`)
   - Просмотр метрик сегментации
   - 3D визуализация печени (Three.js)
   - Интерактивное редактирование 3D модели
   - Скачивание результатов (STL, NIfTI)

3. **Login Page** (`/login`)
   - JWT аутентификация
   - Защита маршрутов через Auth Guard

### 3D визуализация и редактирование:

- **Библиотека**: Three.js
- **Функции редактирования**:
  - Вращение модели
  - Масштабирование
  - Выбор и коррекция отдельных областей
  - Сглаживание поверхности
  - Отмена/повтор действий
  - Экспорт отредактированной модели

---

## 🐳 Docker Deployment

### Сервисы:

```yaml
services:
  db:          # PostgreSQL база данных
  redis:       # Redis для кэширования и очередей
  api:         # FastAPI backend
  frontend:    # Angular frontend (опционально)
  celery:      # Celery worker для фоновых задач
```

### Переменные окружения:

```env
# Backend
DATABASE_URL=postgresql://user:password@db:5432/liver_segmentation
REDIS_URL=redis://redis:6379/0
SECRET_KEY=your-super-secret-key-change-in-production
DEBUG=False
CORS_ORIGINS=http://localhost:4200

# PostgreSQL
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=liver_segmentation

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
```

### Команды Docker:

```bash
# Запустить все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f api

# Остановить все сервисы
docker-compose down

# Пересобрать образы
docker-compose build --no-cache

# Применить миграции БД
docker-compose exec api alembic upgrade head

# Создать админа
docker-compose exec api python scripts/create_admin.py
```

---

## 📊 Метрики и мониторинг

### Клинические метрики качества:

| Метрика | Отлично | Хорошо | Приемлемо | Плохо |
|---------|---------|--------|-----------|-------|
| Dice | ≥0.95 | 0.90-0.95 | 0.85-0.90 | <0.85 |
| IoU | ≥0.90 | 0.85-0.90 | 0.80-0.85 | <0.80 |
| HD95 (mm) | <2 | 2-5 | 5-10 | >10 |
| Volume Error (%) | <2 | 2-5 | 5-10 | >10 |

### Логирование:

- **Backend**: `backend/logs/app_YYYY-MM-DD.log`
- **Security**: `backend/logs/security_YYYY-MM-DD.log`

---

## 📁 Данные заказчика (Anon_Liver)

Папка `Anon_Liver/` содержит КТ-снимки реальных пациентов клиники Екатеринбург.

**Формат данных**:
- DICOM файлы (.dcm)
- Анонимизированные данные пациентов
- Разрешение: обычно 512x512 пикселей
- Толщина среза: 1-5 мм

**Использование**:
1. Для дообучения модели на данных заказчика
2. Для валидации качества сегментации
3. Для тестирования системы

---

## 🔒 Безопасность

### Реализованные меры:

✅ JWT аутентификация с refresh tokens  
✅ Хеширование паролей (pbkdf2_sha256)  
✅ Rate limiting (60 запросов/мин)  
✅ CORS protection  
✅ Input validation (расширения файлов, размер)  
✅ Path traversal protection  
✅ Security headers (HSTS, X-Frame-Options, etc.)  
✅ Логирование безопасности

### Для production:

⚠️ **Обязательно измените**:
- `SECRET_KEY` в `.env`
- Пароли базы данных
- CORS origins
- Включите HTTPS
- Настройте firewall

---

## 🧪 Тестирование

### Backend тесты:

```bash
cd backend
pytest tests/ -v
```

### Frontend тесты:

```bash
cd frontend
npm test
```

### E2E тесты:

```bash
cd frontend
npm run e2e
```

### Нагрузочное тестирование:

```bash
# Установить locust
pip install locust

# Запустить
locust -f tests/load_test.py
```
---

## 🎓 Ссылки и ресурсы

### Документация:
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Angular Documentation](https://angular.io/docs)
- [PyTorch Documentation](https://pytorch.org/docs/)
- [Three.js Documentation](https://threejs.org/docs/)

### Научные статьи:
- [U-Net: Convolutional Networks for Biomedical Image Segmentation](https://arxiv.org/abs/1505.04597)
- [Attention U-Net: Learning Where to Look for the Pancreas](https://arxiv.org/abs/1804.03999)
- [nnU-Net: Self-adapting Framework for U-Net-Based Medical Image Segmentation](https://arxiv.org/abs/1809.10486)

---

## ✅ Статус проекта

- [x] Backend API (FastAPI)
- [x] Frontend (Angular 18)
- [x] 3D U-Net модель для сегментации
- [x] Дообучение на данных заказчика
- [x] 3D визуализация (Three.js)
- [x] Редактирование 3D модели
- [x] Docker контейнеризация
- [x] Метрики качества (Dice ≥ 0.90)
- [x] REST API документация
- [x] Аутентификация и безопасность

**Проект готов к развертыванию и использованию! 🎉**

---

## 🚧 Планы развития

- [ ] Поддержка множественных органов (селезенка, почки)
- [ ] Автоматическое детектирование патологий
- [ ] Интеграция с PACS системами
- [ ] Мобильное приложение
- [ ] Расширенная аналитика и отчеты
